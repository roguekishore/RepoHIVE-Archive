/**
 * @repohive/ui — shared visualization components.
 *
 * Consumers should import via subpath (`@repohive/ui/graph`,
 * `@repohive/ui/chat`, etc.) rather than the barrel below — the barrel
 * exists so the package resolves cleanly in IDEs and tooling that probes
 * the root entry point.
 */

export {};
